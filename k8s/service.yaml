apiVersion: v1
kind: Service
metadata:
  name: mfa-auth-service
spec:
  type: NodePort
  selector:
    app: mfa-auth-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
